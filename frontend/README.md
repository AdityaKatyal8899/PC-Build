
  # Modern Neo-Brutalist UI Design

  This is a code bundle for Modern Neo-Brutalist UI Design. The original project is available at https://www.figma.com/design/8awW0JKKe93muhPUnC5u53/Modern-Neo-Brutalist-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  